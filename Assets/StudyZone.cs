using UnityEngine;

/// <summary>
/// A simple marker script that identifies a Transform as a potential study zone location.
/// It also holds a reference to the active visual indicator.
/// </summary>
public class StudyZone : MonoBehaviour
{
    public GameObject activeVisual;
} 